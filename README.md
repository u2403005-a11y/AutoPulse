# AutoPulse — Luxury AI Used Car Price Prediction

Run the entire project with:

```bash
python app.py
```

This will install Python dependencies, train the ML model (if not present), build the React frontend, start the Flask server, and open the browser.
